import styled from "styled-components";

const CommonRoom = () => {
  return (
    <Wrapper>
      <CommonRoomImage 
      src={src} 
      alt={alt}>
          
      </CommonRoomImage>
    </Wrapper>
  );
};

export default CommonRoom;

const Wrapper = styled.div``;
